export const login = (req, res) => {
  res.json({ message: 'Login endpoint - estructura inicial' });
};

export const logout = (req, res) => {
  res.json({ message: 'Logout endpoint - estructura inicial' });
};

export const register = (req, res) => {
  res.json({ message: 'Register endpoint - estructura inicial' });
};